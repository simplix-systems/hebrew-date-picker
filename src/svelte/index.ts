// Svelte entry. Built with `svelte-package`.
// Core utilities (setGlobalConfig, calendar helpers, types) live in the main
// entry: `import { setGlobalConfig } from 'hebrew-datepicker'`.
export { default as HebrewDatePicker } from './HebrewDatePicker.svelte';
